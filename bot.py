import telebot
from telebot import types
import os
import sys
import logging
import time
import re

# Настройка логирования для Railway
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO
)
logger = logging.getLogger(__name__)

# Добавляем путь для локальных модулей
sys.path.append(os.path.dirname(os.path.abspath(__file__)))

# Пытаемся импортировать наши модули
try:
    import download_schedule
    import schedule_parser
    LOCAL_MODULES = True
    logger.info("✅ Локальные модули загружены")
except ImportError as e:
    logger.error(f"⚠️  Не удалось импортировать локальные модули: {e}")
    LOCAL_MODULES = False

# Получаем токен из переменных окружения Railway
TOKEN = os.getenv('TELEGRAM_BOT_TOKEN')

if not TOKEN:
    logger.error("❌ Токен не найден! Установите переменную TELEGRAM_BOT_TOKEN в Railway")
    sys.exit(1)

logger.info(f"✅ Токен получен: {TOKEN[:10]}...")

# Создаем бота
bot = telebot.TeleBot(TOKEN)

# ====== КОНСТАНТЫ ======
ADMIN_IDS = []  # Добавьте сюда ID администраторов через запятую, например: [123456789, 987654321]

# ====== ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ ======

def update_schedule_file():
    """Обновляет файл расписания"""
    if not LOCAL_MODULES:
        return False, "Модули расписания не загружены"
    
    try:
        logger.info("🔄 Начинаю обновление расписания...")
        download_schedule.download_schedule_from_site()
        
        import importlib
        importlib.reload(schedule_parser)
        
        if os.path.exists('school_schedule.csv'):
            file_size = os.path.getsize('school_schedule.csv')
            return True, f"Расписание обновлено! Размер файла: {file_size} байт"
        else:
            return False, "Файл расписания не был создан"
    except Exception as e:
        return False, f"Ошибка: {str(e)}"

def create_main_keyboard():
    """Создает основную клавиатуру с кнопками"""
    keyboard = types.ReplyKeyboardMarkup(resize_keyboard=True)
    
    buttons = [
        "📋 Найти класс",
        "👨‍🏫 Найти учителя",
        "🔄 Обновить",
        "❓ Помощь",
        "ℹ️ О боте"
    ]
    
    # Добавляем кнопки по одной в ряд
    for button in buttons:
        keyboard.add(types.KeyboardButton(button))
    
    return keyboard

def create_classes_keyboard():
    """Создает клавиатуру для поиска класса"""
    keyboard = types.ReplyKeyboardMarkup(resize_keyboard=True)
    keyboard.add(types.KeyboardButton("⬅️ Назад"))
    return keyboard

def create_teachers_keyboard():
    """Создает клавиатуру для поиска учителя"""
    keyboard = types.ReplyKeyboardMarkup(resize_keyboard=True)
    keyboard.add(types.KeyboardButton("⬅️ Назад"))
    return keyboard

def create_back_keyboard():
    """Создает клавиатуру только с кнопкой Назад"""
    keyboard = types.ReplyKeyboardMarkup(resize_keyboard=True)
    keyboard.add(types.KeyboardButton("⬅️ Назад"))
    return keyboard

# ====== ОБРАБОТЧИКИ КОМАНД ======

@bot.message_handler(commands=['start', 'help'])
def send_welcome(message):
    """Обработчик команд /start и /help"""
    welcome_text = (
        "👋 *Добро пожаловать в школьный бот расписания!*\n\n"
        "Я помогу вам быстро найти расписание уроков.\n\n"
        "🎯 *Основные возможности:*\n"
        "• Поиск расписания по классу\n"
        "• Поиск расписания по учителю\n"
        "• Автоматическое обновление данных\n\n"
        "📱 *Используйте кнопки ниже для навигации*\n\n"
        "💡 *Совет:* Начните с кнопки 'Найти класс' или 'Найти учителя'"
    )
    
    bot.send_message(
        message.chat.id,
        welcome_text,
        parse_mode='Markdown',
        reply_markup=create_main_keyboard()
    )

@bot.message_handler(commands=['update'])
def update_command(message):
    """Обновление расписания"""
    if message.from_user.id not in ADMIN_IDS and ADMIN_IDS:
        bot.reply_to(message, "❌ Эта команда доступна только администраторам.")
        return
    
    bot.send_message(
        message.chat.id,
        "🔄 Обновляю расписание с сайта...",
        reply_markup=create_back_keyboard()
    )
    
    success, msg = update_schedule_file()
    
    if success:
        bot.send_message(message.chat.id, f"✅ {msg}", parse_mode='Markdown')
    else:
        bot.send_message(message.chat.id, f"❌ {msg}", parse_mode='Markdown')

@bot.message_handler(commands=['schedule', 'class'])
def schedule_command(message):
    """Запрос расписания класса"""
    bot.send_message(
        message.chat.id,
        "📋 *Введите номер класса:*\n\n"
        "Например: 5А, 10Е, 8 Б\n\n"
        "💡 Просто отправьте номер класса",
        parse_mode='Markdown',
        reply_markup=create_classes_keyboard()
    )

@bot.message_handler(commands=['classes'])
def classes_command(message):
    """Список всех классов"""
    if not LOCAL_MODULES:
        bot.send_message(message.chat.id, "❌ Модули не загружены")
        return
    
    try:
        classes = schedule_parser.get_available_classes()
        if classes:
            # Разделяем классы по параллелям
            classes_by_grade = {}
            for cls in classes:
                match = re.search(r'(\d+)([А-Я])', cls)
                if match:
                    grade = match.group(1)
                    if grade not in classes_by_grade:
                        classes_by_grade[grade] = []
                    classes_by_grade[grade].append(cls)
            
            # Формируем сообщение
            text = "📋 *Все доступные классы:*\n\n"
            for grade in sorted(classes_by_grade.keys(), key=int):
                text += f"*{grade} класс:* {', '.join(sorted(classes_by_grade[grade]))}\n"
            
            text += f"\n📊 Всего: {len(classes)} классов"
            
            bot.send_message(message.chat.id, text, parse_mode='Markdown')
        else:
            bot.send_message(message.chat.id, 
                           "❌ Классы не найдены. Используйте /update", 
                           parse_mode='Markdown')
    except Exception as e:
        bot.send_message(message.chat.id, f"❌ Ошибка: {str(e)}", parse_mode='Markdown')

@bot.message_handler(commands=['teacher'])
def teacher_command(message):
    """Поиск расписания по учителю"""
    args = message.text.split()
    if len(args) < 2:
        bot.send_message(
            message.chat.id,
            "👨‍🏫 *Поиск расписания учителя:*\n\n"
            "✏️ *Введите фамилию учителя:*\n"
            "Например: Протасова\n\n"
            "🔍 *Совет:* Можно ввести первые буквы фамилии\n"
            "Например: про\n\n"
            "✏️ Введите фамилию учителя или первые буквы фамилии",
            parse_mode='Markdown',
            reply_markup=create_teachers_keyboard()
        )
        return
    
    teacher_name = ' '.join(args[1:])
    search_teacher_schedule(message, teacher_name)

@bot.message_handler(commands=['teachers'])
def search_teachers_command(message):
    """Поиск учителей по части фамилии"""
    args = message.text.split()
    if len(args) < 2:
        bot.send_message(
            message.chat.id,
            "🔍 *Поиск учителей:*\n\n"
            "✏️ *Введите часть фамилии:*\n"
            "Например: про\n\n"
            "📝 *Найдет:* Протасова, Прокопьев и т.д.\n\n"
            "⚠️ *Важно:* В оригинальном расписании могут быть опечатки, "
            "поэтому лучше искать по первым символам фамилии.",
            parse_mode='Markdown',
            reply_markup=create_back_keyboard()
        )
        return
    
    search_query = args[1]
    search_teacher_by_partial(message, search_query)

@bot.message_handler(commands=['about', 'info'])
def about_command(message):
    """Информация о боте"""
    about_text = (
        "ℹ️ *Информация о боте:*\n\n"
        "🤖 *Школьный бот расписания*\n"
        "Версия: 2.0\n\n"
        "📊 *Функционал:*\n"
        "• Поиск расписания по классам\n"
        "• Поиск расписания по учителям\n"
        "• Автоматическое обновление данных\n"
        "• Удобный интерфейс с кнопками\n\n"
        "⚠️ *Важная информация:*\n"
        "1. Данные берутся с официального сайта школы\n"
        "2. В расписании могут быть опечатки\n"
        "3. Рекомендуем искать учителей по первым символам фамилии\n"
        "4. Бот не несёт ответственности за неточности в расписании\n\n"
        "🎓 *Бот создан для образовательных целей*\n"
        "Тестирование показало хорошие результаты работы\n\n"
        "🔧 *Техническая информация:*\n"
        "• Данные обновляются командой /update\n"
        "• Работает на платформе Railway\n"
        "• Исходный код: закрытый\n\n"
        "📞 *Поддержка:*\n"
        "По вопросам работы бота обращайтесь к администратору."
    )
    
    bot.send_message(
        message.chat.id,
        about_text,
        parse_mode='Markdown',
        reply_markup=create_main_keyboard()
    )

@bot.message_handler(commands=['stats'])
def stats_command(message):
    """Статистика бота"""
    if not LOCAL_MODULES:
        bot.send_message(message.chat.id, "❌ Модули не загружены")
        return
    
    try:
        # Получаем статистику
        classes = schedule_parser.get_available_classes()
        teacher_index = schedule_parser.get_cached_teacher_index()
        
        # Проверяем файл расписания
        file_exists = schedule_parser.has_schedule_file()
        file_info = ""
        
        if file_exists and os.path.exists('school_schedule.csv'):
            file_size = os.path.getsize('school_schedule.csv')
            file_info = f"Размер файла: {file_size} байт\n"
        
        stats_text = (
            f"📊 *Статистика бота:*\n\n"
            f"📋 *Классы:* {len(classes) if classes else 0}\n"
            f"👨‍🏫 *Учителя:* {len(teacher_index) if teacher_index else 0}\n"
            f"{file_info}"
            f"🔄 *Последнее обновление:* {time.strftime('%d.%m.%Y %H:%M')}\n\n"
            f"✅ *Статус:* {'Работает нормально' if file_exists else 'Требуется обновление'}\n\n"
            f"💡 Используйте /update для обновления данных"
        )
        
        bot.send_message(message.chat.id, stats_text, parse_mode='Markdown')
        
    except Exception as e:
        bot.send_message(message.chat.id, f"❌ Ошибка: {str(e)}", parse_mode='Markdown')

@bot.message_handler(commands=['manual', 'guide', 'инструкция'])
def manual_command(message):
    """Полная инструкция по использованию бота"""
    manual_text = (
        "📖 *Полное руководство пользователя*\n\n"
        
        "🎯 *Основное назначение бота:*\n"
        "Бот предназначен для быстрого поиска школьного расписания "
        "по классам и учителям. *Проект создан для образовательных целей.*\n\n"
        
        "⚠️ *ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ:*\n"
        "1. *Опечатки в исходных данных:* В оригинальном расписании "
        "могут быть ошибки в написании фамилий учителей.\n"
        "2. *Рекомендация по поиску:* Используйте первые 2-3 символа "
        "фамилии для поиска учителей.\n"
        "3. *Ответственность:* Бот не несёт ответственности за "
        "неточности в выводимом расписании.\n"
        "4. *Образовательный проект:* Бот создан для демонстрации "
        "работы с данными и тестирования алгоритмов поиска.\n\n"
        
        "🔍 *КАК ПОЛЬЗОВАТЬСЯ БОТОМ:*\n\n"
        
        "📋 *Поиск расписания класса:*\n"
        "1. Нажмите кнопку '📋 Найти класс'\n"
        "2. Введите номер класса (например: 5А, 10Е, 8 Б)\n"
        "3. Бот покажет расписание на день\n\n"
        
        "👨‍🏫 *Поиск расписания учителя:*\n"
        "1. Нажмите кнопку '👨‍🏫 Найти учителя'\n"
        "2. *Введите первые буквы фамилии* (например: 'про')\n"
        "3. Если точная фамилия не найдена, используйте команду "
        "/teachers <часть_фамилии>\n"
        "4. *Из-за опечаток в расписании поиск по полной фамилии "
        "может не сработать!*\n\n"
        
        "🎯 *ПРАВИЛЬНЫЙ ПОИСК УЧИТЕЛЯ:*\n"
        "❌ *Неправильно:* /teacher Протасова (может не найти из-за опечаток)\n"
        "✅ *Правильно:* /teacher про (найдет всех с 'про' в фамилии)\n"
        "✅ *Правильно:* /teachers про (покажет список всех найденных)\n\n"
        
        "🔄 *Обновление данных:*\n"
        "• Данные обновляются автоматически\n"
        "• Принудительное обновление: кнопка '🔄 Обновить'\n"
        "• Только для администраторов\n\n"
        
        "📊 *Статистика и информация:*\n"
        "• /stats - статистика работы бота\n"
        "• /about - информация о боте и предупреждения\n"
        "• /classes - полный список всех классов\n\n"
        
        "🚨 *ЧТО ДЕЛАТЬ, ЕСЛИ НЕ НАХОДИТ:*\n"
        "1. Проверьте, нет ли опечаток в запросе\n"
        "2. Используйте поиск по части фамилии (первые буквы)\n"
        "3. Попробуйте разные варианты написания\n"
        "4. Обновите данные командой /update (если есть доступ)\n"
        "5. Обратитесь к администратору\n\n"
        
        "✅ *ЧТО МЫ ПРОТЕСТИРОВАЛИ:*\n"
        "• Поиск классов работает стабильно\n"
        "• Поиск учителей по части фамилии дает хорошие результаты\n"
        "• Автоматическое обновление работает корректно\n"
        "• Бот стабильно работает на платформе Railway\n\n"
        
        "📞 *Поддержка:*\n"
        "По вопросам работы бота, предложениям и ошибкам "
        "обращайтесь к администратору проекта.\n\n"
        
        "🎓 *Бот создан в образовательных целях.*\n"
        "Тестирование показало хорошие результаты работы системы."
    )
    
    bot.send_message(
        message.chat.id,
        manual_text,
        parse_mode='Markdown',
        reply_markup=create_main_keyboard()
    )

# ====== ОБРАБОТЧИКИ КНОПОК ======

@bot.message_handler(func=lambda message: message.text == "📋 Найти класс")
def handle_find_class_button(message):
    """Обработка кнопки 'Найти класс'"""
    schedule_command(message)

@bot.message_handler(func=lambda message: message.text == "👨‍🏫 Найти учителя")
def handle_find_teacher_button(message):
    """Обработка кнопки 'Найти учителя'"""
    teacher_command(message)

@bot.message_handler(func=lambda message: message.text == "🔄 Обновить")
def handle_update_button(message):
    """Обработка кнопки 'Обновить'"""
    update_command(message)

@bot.message_handler(func=lambda message: message.text == "❓ Помощь")
def handle_help_button(message):
    """Обработка кнопки 'Помощь'"""
    help_text = (
        "❓ *Помощь по использованию бота:*\n\n"
        
        "🎯 *Как найти расписание класса:*\n"
        "1. Нажмите кнопку '📋 Найти класс'\n"
        "2. Введите номер класса\n"
        "3. Например: 5А, 10Е, 8 Б\n\n"
        
        "👨‍🏫 *Как найти расписание учителя:*\n"
        "1. Нажмите кнопку '👨‍🏫 Найти учителя'\n"
        "2. Введите фамилию учителя или первые буквы\n"
        "3. *Важно:* В расписании могут быть опечатки\n"
        "4. *Совет:* Ищите по первым символам фамилии\n"
        "   Например: 'про' для Протасова\n\n"
        
        "🔍 *Поиск учителя по части фамилии:*\n"
        "• Отправьте команду /teachers <часть>\n"
        "• Например: /teachers про\n"
        "• Бот покажет всех учителей с такой частью фамилии\n\n"
        
        "⚠️ *Важная информация:*\n"
        "• Данные автоматически обновляются\n"
        "• В расписании возможны опечатки\n"
        "• Бот не несёт ответственности за неточности\n"
        "• Проект создан для образовательных целей\n\n"
        
        "📞 *Если возникли проблемы:*\n"
        "1. Попробуйте обновить данные (/update)\n"
        "2. Проверьте написание класса/фамилии\n"
        "3. Используйте поиск по части фамилии\n"
        "4. Обратитесь к администратору\n\n"
        
        "💡 *Быстрые команды:*\n"
        "/start - главное меню\n"
        "/help - эта справка\n"
        "/about - информация о боте\n"
        "/stats - статистика\n"
        "/classes - все классы\n"
        "/teacher <фамилия> - найти учителя\n"
        "/teachers <часть> - поиск учителей\n"
        "/manual - полное руководство"
    )
    
    bot.send_message(
        message.chat.id,
        help_text,
        parse_mode='Markdown',
        reply_markup=create_main_keyboard()
    )

@bot.message_handler(func=lambda message: message.text == "ℹ️ О боте")
def handle_about_button(message):
    """Обработка кнопки 'О боте'"""
    about_command(message)

@bot.message_handler(func=lambda message: message.text == "⬅️ Назад")
def handle_back_button(message):
    """Обработка кнопки 'Назад'"""
    bot.send_message(
        message.chat.id,
        "🔙 Возвращаюсь в главное меню...",
        reply_markup=create_main_keyboard()
    )

# ====== ОБРАБОТЧИКИ ТЕКСТА ======

@bot.message_handler(func=lambda message: True)
def handle_text(message):
    """Обработка текстовых сообщений"""
    user_input = message.text.strip()
    
    if not LOCAL_MODULES:
        bot.send_message(message.chat.id, "❌ Модули не загружены")
        return
    
    if not schedule_parser.has_schedule_file():
        bot.send_message(
            message.chat.id,
            "❌ *Файл расписания не найден!*\n\n"
            "📥 Используйте команду /update чтобы скачать актуальное расписание.",
            parse_mode='Markdown',
            reply_markup=create_main_keyboard()
        )
        return
    
    try:
        # Проверяем, является ли ввод классом (цифра + буква)
        if re.match(r'^\d+\s*[А-Яа-яA-Za-z]$', user_input, re.IGNORECASE):
            # Это класс
            search_class_schedule(message, user_input)
        else:
            # Пробуем найти как учителя
            search_teacher_schedule(message, user_input)
            
    except Exception as e:
        logger.error(f"Ошибка обработки запроса '{user_input}': {e}")
        bot.send_message(
            message.chat.id,
            f"❌ *Ошибка при обработке запроса:* {str(e)}\n\n"
            "💡 *Попробуйте:*\n"
            "1. Проверить написание\n"
            "2. Обновить расписание /update\n"
            "3. Обратиться к администратору",
            parse_mode='Markdown',
            reply_markup=create_main_keyboard()
        )

# ====== ФУНКЦИИ ПОИСКА ======

def search_class_schedule(message, class_name):
    """Поиск расписания для класса"""
    try:
        lessons = schedule_parser.get_schedule_for_class(class_name)
        
        if lessons is None:
            bot.send_message(
                message.chat.id,
                f"❌ Класс *{class_name}* не найден.\n\n"
                "💡 *Попробуйте:*\n"
                "• Другой формат (5А, 5 А, 5а)\n"
                "• Команду /classes для списка всех классов\n"
                "• Обновить расписание /update",
                parse_mode='Markdown',
                reply_markup=create_classes_keyboard()
            )
            return
        
        # Форматируем расписание
        message_text = schedule_parser.format_schedule_for_telegram(class_name, lessons)
        
        # Добавляем предупреждение
        message_text += (
            "\n\n⚠️ *Обратите внимание:*\n"
            "Расписание может содержать опечатки или изменения.\n"
            "Актуальную информацию уточняйте у классного руководителя."
        )
        
        bot.send_message(
            message.chat.id,
            message_text,
            parse_mode='Markdown',
            reply_markup=create_classes_keyboard()
        )
        
    except Exception as e:
        logger.error(f"Ошибка поиска класса {class_name}: {e}")
        bot.send_message(
            message.chat.id,
            f"❌ *Ошибка при поиске класса:* {str(e)}\n"
            "Попробуйте обновить расписание командой /update",
            parse_mode='Markdown',
            reply_markup=create_main_keyboard()
        )

def search_teacher_schedule(message, teacher_name):
    """Поиск расписания для учителя"""
    try:
        teacher_info = schedule_parser.get_schedule_by_teacher(teacher_name)
        
        if not teacher_info:
            # Предлагаем поиск по части фамилии
            bot.send_message(
                message.chat.id,
                f"❌ Учитель *{teacher_name}* не найден.\n\n"
                "💡 *Рекомендации:*\n"
                "1. *Проверьте написание* - в расписании могут быть опечатки\n"
                "2. *Ищите по первым символам* - например, 'про' для 'Протасова'\n"
                "3. *Используйте поиск:* /teachers {первые_буквы}\n"
                "4. *Попробуйте другие варианты* написания фамилии\n\n"
                "⚠️ Из-за особенностей исходного расписания "
                "поиск по полной фамилии может не работать.",
                parse_mode='Markdown',
                reply_markup=create_teachers_keyboard()
            )
            return
        
        # Форматируем расписание
        response_text = schedule_parser.format_teacher_schedule(teacher_info)
        
        # Добавляем важное предупреждение
        response_text += (
            "\n\n⚠️ *Внимание:*\n"
            "1. В оригинальном расписании могут быть *опечатки*\n"
            "2. Рекомендуем искать учителей по *первым символам* фамилии\n"
            "3. Бот не несёт ответственности за неточности\n"
            "4. Проект создан для *образовательных целей*\n\n"
            "✅ Тестирование показало хорошие результаты работы"
        )
        
        bot.send_message(
            message.chat.id,
            response_text,
            parse_mode='Markdown',
            reply_markup=create_teachers_keyboard()
        )
        
    except Exception as e:
        logger.error(f"Ошибка поиска учителя {teacher_name}: {e}")
        bot.send_message(
            message.chat.id,
            f"❌ *Ошибка при поиске учителя:* {str(e)}\n\n"
            "💡 *Совет:* Попробуйте поиск по части фамилии: /teachers {первые_буквы}",
            parse_mode='Markdown',
            reply_markup=create_main_keyboard()
        )

def search_teacher_by_partial(message, search_query):
    """Поиск учителей по части фамилии"""
    try:
        matches = schedule_parser.search_teachers_by_substring(search_query)
        response_text = schedule_parser.format_teachers_search_results(matches, search_query)
        
        # Добавляем рекомендации
        response_text += (
            "\n\n⚠️ *Важные рекомендации:*\n"
            "1. В расписании возможны *опечатки* в фамилиях\n"
            "2. *Ищите по первым символам* фамилии (2-3 буквы)\n"
            "3. Если не нашли - попробуйте другой вариант написания\n"
            "4. Для полного расписания: /teacher <фамилия>\n\n"
            "🎓 *Бот создан для образовательных целей*"
        )
        
        bot.send_message(
            message.chat.id,
            response_text,
            parse_mode='Markdown',
            reply_markup=create_teachers_keyboard()
        )
        
    except Exception as e:
        logger.error(f"Ошибка поиска учителей {search_query}: {e}")
        bot.send_message(
            message.chat.id,
            f"❌ Ошибка: {str(e)}",
            parse_mode='Markdown',
            reply_markup=create_main_keyboard()
        )

# ====== ЗАПУСК БОТА ======

def main():
    """Основная функция"""
    logger.info("=" * 60)
    logger.info("🤖 ШКОЛЬНЫЙ БОТ ЗАПУСКАЕТСЯ НА RAILWAY")
    logger.info("=" * 60)
    
    # Проверяем файл расписания
    if LOCAL_MODULES:
        if os.path.exists('school_schedule.csv'):
            logger.info("✅ Файл расписания найден")
            
            # Инициализируем кэш учителей при запуске
            try:
                teacher_index = schedule_parser.get_cached_teacher_index()
                logger.info(f"✅ Индекс учителей создан: {len(teacher_index)} учителей")
            except Exception as e:
                logger.error(f"⚠️ Ошибка создания индекса учителей: {e}")
        else:
            logger.info("📭 Файл расписания не найден")
            logger.info("ℹ️  Используйте /update в боте для загрузки")
    
    # Запускаем бота с перезапуском при ошибках
    while True:
        try:
            logger.info("🔄 Запуск polling...")
            bot.polling(none_stop=True, interval=2, timeout=30)
        except Exception as e:
            logger.error(f"❌ Ошибка polling: {e}")
            logger.info("⏳ Перезапуск через 10 секунд...")
            time.sleep(10)

if __name__ == '__main__':
    main()